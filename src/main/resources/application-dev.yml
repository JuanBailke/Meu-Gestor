spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/meu_gestor
    username: admin
    password: admin_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true